// 22-modules-1-math.ts
/**
 * Aufgaben:
 * 1) Exportiere `clamp(value, min, max)`
 * 2) Exportiere `round2(value)` (2 Nachkommastellen)
 */
export function clamp(value, min, max) {
    // TODO
    return value;
}
export function round2(value) {
    // TODO
    return value;
}
